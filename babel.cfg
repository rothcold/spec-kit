# Babel configuration for message extraction
# This file defines which files to scan for translatable strings

[python: **.py]
# Extract messages from Python source files
# Marker function: _()

[markdown: templates/**.md]
# Extract messages from template markdown files (for future use)
# Currently templates are translated as complete files, not inline

[markdown: docs/**.md]
# Extract messages from documentation markdown files (for future use)
